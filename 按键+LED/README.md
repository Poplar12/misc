# 按键和LED处理

## 介绍
1.  按键通过获取系统的时间因子(需要将获取时间注册进去), 结合按键状态, 按键释放后, 给出按键的状态: 短按, 200ms, 1s, 双击

2.  LED的设计, 通过外部发送LED状态到LED队列, LED收到状态后, 查询LED显示状态表, 进行相应的显示.在LED的显示中充分体现了多态的实现, 设计者只需要在状态表新增状态, 就可实现新的状态显示, 如果使用switch, 新的状态的加入势必会导致结构混乱, 因为新的状态可能需要修改到之前的代码.多态是实现高内聚的一种非常好的手段.

